import AddUserPopup from "@/components/forms/addUserForm";

export default async function Home({ ...props }) {
  return (
    <main className="min-h-screen flex-col w-screen items-center justify-center"></main>
  );
}
