import NavContent from "./NavContent";
import SignOut from "./SignOut";

const Nav = () => {
  return (
    <nav className="flex p-8 justify-between">
      <NavContent />
    </nav>
  );
};

export default Nav;
